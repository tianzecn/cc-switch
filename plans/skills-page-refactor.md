# Skills 页面重构规格说明

## 📋 概述

将 Skills 管理页面重构为与 Commands 页面一致的双栏布局结构，引入命名空间概念，统一 UI 体验。

## 🎯 目标

1. **统一布局**：Skills 页面采用与 Commands 一致的双栏布局（左侧树形导航 + 右侧列表）
2. **引入命名空间**：为 Skills 添加命名空间支持，实现三级结构（仓库 → 命名空间 → Skill）
3. **合并组件**：将 UnifiedSkillsPanel 和 SkillsPage 合并为统一的 SkillsPage 组件
4. **一致性体验**：确保 Skills、Commands、Hooks、Agents 页面的 UI 体验一致

---

## 📐 架构设计

### 组件结构

```
SkillsPage (重构后)
├── viewMode: "list" | "discovery" | "import"
├── Header（顶部工具栏）
│   ├── 搜索框
│   ├── 筛选器（已安装/未安装/全部）
│   └── 操作按钮（刷新、发现、导入）
├── Stats Bar（统计栏）
├── ConflictDetectionPanel（冲突检测面板）
└── Main Content（双栏布局）
    ├── Left: SkillNamespaceTree（w-64）
    │   ├── 顶部工具栏（+ 添加仓库）
    │   ├── "全部 Skills" 节点
    │   └── 仓库树（仓库 → 命名空间 两级）
    └── Right: SkillsList（flex-1）
        ├── 列表项（SkillListItem）
        └── 详情面板（SkillDetailPanel，w-80，选中时显示）
```

### 数据流

```
App.tsx
  └── SkillsPage
        ├── useInstalledSkills()      → 已安装 Skills
        ├── useDiscoverableSkills()   → 可发现 Skills
        ├── useSkillNamespaces()      → 命名空间列表（新增）
        ├── useSkillRepos()           → 仓库列表
        └── 各种 mutations...
```

---

## 🗃️ 数据模型变更

### InstalledSkill（后端）

```rust
// src-tauri/src/models/skill.rs
pub struct InstalledSkill {
    pub id: String,
    pub name: String,
    pub description: Option<String>,
    pub directory: String,
    pub namespace: String,           // 新增：命名空间
    pub repo_owner: Option<String>,
    pub repo_name: Option<String>,
    pub readme_url: Option<String>,
    pub apps: AppFlags,
    pub created_at: DateTime<Utc>,
    pub updated_at: DateTime<Utc>,
}
```

### 数据库迁移

```sql
-- 添加 namespace 列
ALTER TABLE installed_skills ADD COLUMN namespace TEXT NOT NULL DEFAULT '';

-- 迁移现有数据：从 directory 推断命名空间
-- 规则：directory 的父目录名作为命名空间
-- 例如：skills/my-namespace/skill-name → namespace = "my-namespace"
-- 如果是根目录，则 namespace = ""（根命名空间）
```

### 命名空间推断规则

1. **从目录结构推断**：
   - `~/.claude/commands/skills/namespace/skill-dir` → `namespace = "namespace"`
   - `~/.claude/commands/skills/skill-dir` → `namespace = ""`（根命名空间）

2. **安装时**：
   - 远程仓库：从仓库目录结构推断
   - 本地创建：用户可选择或创建命名空间

---

## 🎨 UI 规格

### 页面布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  Container: max-w-[72rem] h-[calc(100vh-8rem)]                              │
├─────────────────────────────────────────────────────────────────────────────┤
│  Header: py-4                                                               │
│  ┌──────────────────────────┬──────────────────────────────────────────────┐│
│  │ 图标 + 标题 + 描述        │ [搜索框] [筛选▼] [刷新] [导入] [发现]        ││
│  └──────────────────────────┴──────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────────────────────┤
│  Stats Bar: glass rounded-xl mb-4                                           │
│  │ 已安装: X · Claude: X · Codex: X · Gemini: X                            ││
├─────────────────────────────────────────────────────────────────────────────┤
│  ConflictDetectionPanel: mb-4 (如有同名 Skill 冲突)                         │
├─────────────────────────────────────────────────────────────────────────────┤
│  Main Content: flex-1 flex gap-4 overflow-hidden                            │
│  ┌──────────────┐  ┌──────────────────────────────────────────────────────┐│
│  │NamespaceTree │  │SkillsList                                            ││
│  │w-64          │  │flex-1                                                ││
│  │              │  │┌─────────────────────────────────┐ ┌────────────────┐││
│  │[+ 添加仓库]   │  ││ 列表 (flex-1)                   │ │DetailPanel    │││
│  │──────────────│  ││ ┌─────────────────────────────┐ │ │w-80           │││
│  │📁 全部    (15)│  ││ │ ✓ skill1 [开关x3] [🗑️]    │ │ │               │││
│  │▼💻本地    (5) │  ││ └─────────────────────────────┘ │ │ README 预览   │││
│  │ │📁根     (3) │  ││ ┌─────────────────────────────┐ │ │ 文件列表      │││
│  │ │📁ns1    (2) │  ││ │   skill2 [开关x3] [🗑️]    │ │ │ [编辑器打开]  │││
│  │▶🔗repo   (10) │  ││ └─────────────────────────────┘ │ │               │││
│  │              │  │└─────────────────────────────────┘ └────────────────┘││
│  └──────────────┘  └──────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────────┘
```

### SkillNamespaceTree 组件

```tsx
interface SkillNamespaceTreeProps {
  namespaces: SkillNamespace[];
  skills?: InstalledSkill[];
  repos: SkillRepo[];
  selectedNamespace: string | null;
  onSelectNamespace: (namespace: string | null) => void;
  onAddRepo: (repo: SkillRepo) => void;
  onRemoveRepo: (owner: string, name: string) => void;
}

// 树结构
- 顶部工具栏
  - [+ 添加仓库] 按钮 → 弹出添加仓库表单
- "全部 Skills" 节点
- 仓库节点（可展开）
  - 本地 Skills（绿色图标）
    - 命名空间节点
  - 远程仓库（蓝色图标，可删除）
    - 命名空间节点
```

### SkillListItem 组件

```tsx
interface SkillListItemProps {
  skill: InstalledSkill | DiscoverableSkill;
  isSelected: boolean;
  isInstalled: boolean;        // 发现页面使用
  onSelect: () => void;
  onToggleApp: (app: AppType, enabled: boolean) => void;
  onUninstall: () => void;
  onInstall?: () => void;      // 发现页面使用
}

// 布局
┌─────────────────────────────────────────────────────────────────┐
│ [✓] │ 名称 + 描述 + 来源标签    │ Claude ○  │ [🗑️]            │
│     │                          │ Codex  ○  │                  │
│     │                          │ Gemini ○  │                  │
└─────────────────────────────────────────────────────────────────┘

// 安装状态指示
- 已安装：左侧显示绿色 ✓ 图标
- 未安装：左侧显示灰色 ○ 图标，悬停显示 [安装] 按钮
```

### SkillDetailPanel 组件

```tsx
interface SkillDetailPanelProps {
  skill: InstalledSkill | DiscoverableSkill;
  onClose: () => void;
  onOpenEditor?: () => void;
}

// 内容
- 关闭按钮
- Skill 名称 + 描述
- 元数据（来源、安装时间、目录路径）
- README.md 预览（如果有）
- 文件列表
- [在编辑器中打开] 按钮（已安装时）
- [安装] 按钮（未安装时）
```

### ConflictDetectionPanel 组件

```tsx
interface SkillConflictDetectionPanelProps {
  className?: string;
}

// 功能
- 检测不同仓库中是否有同名 Skill
- 检测同一命名空间下是否有重复 Skill
- 显示冲突列表和解决建议
```

---

## 📁 文件结构

### 新增文件

```
src/components/skills/
├── SkillsPage.tsx              # 重构后的主页面（合并原有两个）
├── SkillNamespaceTree.tsx      # 左侧命名空间树组件
├── SkillsList.tsx              # 右侧列表组件
├── SkillListItem.tsx           # 列表项组件
├── SkillDetailPanel.tsx        # 详情面板组件
├── SkillConflictPanel.tsx      # 冲突检测面板
└── SkillDiscovery.tsx          # 发现视图（可能合并到主页面）

src/hooks/
└── useSkills.ts                # 添加 useSkillNamespaces hook
```

### 删除/弃用文件

```
src/components/skills/
├── UnifiedSkillsPanel.tsx      # 删除，功能合并到 SkillsPage
├── SkillCard.tsx               # 删除，改用 SkillListItem
└── RepoManagerPanel.tsx        # 删除，功能集成到 SkillNamespaceTree
```

---

## 🔧 后端变更

### 新增 API

```rust
// src-tauri/src/commands/skill.rs

#[tauri::command]
pub async fn get_skill_namespaces() -> Result<Vec<SkillNamespace>, String>;

#[tauri::command]
pub async fn create_skill_namespace(name: String) -> Result<SkillNamespace, String>;

#[tauri::command]
pub async fn delete_skill_namespace(name: String) -> Result<(), String>;

#[tauri::command]
pub async fn detect_skill_conflicts() -> Result<Vec<SkillConflict>, String>;
```

### 数据库 DAO

```rust
// src-tauri/src/database/dao/skill_dao.rs

impl SkillDao {
    pub fn get_namespaces(&self) -> Result<Vec<SkillNamespace>>;
    pub fn create_namespace(&self, name: &str) -> Result<SkillNamespace>;
    pub fn delete_namespace(&self, name: &str) -> Result<()>;
    pub fn get_skills_by_namespace(&self, namespace: &str) -> Result<Vec<InstalledSkill>>;
}
```

---

## 📋 任务清单

### Phase 1: 后端数据模型 (优先级: 高)

- [ ] 1.1 修改 InstalledSkill 结构，添加 namespace 字段
- [ ] 1.2 编写数据库迁移脚本
- [ ] 1.3 实现命名空间推断逻辑
- [ ] 1.4 添加 get_skill_namespaces API
- [ ] 1.5 添加 create/delete_skill_namespace API
- [ ] 1.6 添加 detect_skill_conflicts API

### Phase 2: 前端 Hooks (优先级: 高)

- [ ] 2.1 添加 useSkillNamespaces hook
- [ ] 2.2 更新 useInstalledSkills 返回 namespace 字段
- [ ] 2.3 添加 useCreateSkillNamespace hook
- [ ] 2.4 添加 useDeleteSkillNamespace hook
- [ ] 2.5 添加 useSkillConflicts hook

### Phase 3: 核心组件 (优先级: 高)

- [ ] 3.1 创建 SkillNamespaceTree 组件
- [ ] 3.2 创建 SkillsList 组件
- [ ] 3.3 创建 SkillListItem 组件
- [ ] 3.4 创建 SkillDetailPanel 组件
- [ ] 3.5 创建 SkillConflictPanel 组件

### Phase 4: 页面重构 (优先级: 高)

- [ ] 4.1 重构 SkillsPage 为双栏布局
- [ ] 4.2 实现 viewMode 切换 (list/discovery/import)
- [ ] 4.3 集成搜索框和筛选器到 Header
- [ ] 4.4 集成仓库管理到左侧树

### Phase 5: App.tsx 集成 (优先级: 中)

- [ ] 5.1 更新 App.tsx 中的 Skills 视图逻辑
- [ ] 5.2 移除 UnifiedSkillsPanel 引用
- [ ] 5.3 更新 UnifiedNavbar 的 pageActionRefs

### Phase 6: 清理与优化 (优先级: 低)

- [ ] 6.1 删除废弃的组件文件
- [ ] 6.2 更新 i18n 翻译
- [ ] 6.3 代码格式化和类型检查
- [ ] 6.4 功能测试

---

## ⚠️ 注意事项

1. **数据迁移风险**：迁移现有数据时需要确保不丢失用户配置
2. **向后兼容**：迁移后的数据格式需要兼容旧版本（如果需要回滚）
3. **命名空间唯一性**：同一仓库下命名空间名称必须唯一
4. **UI 一致性**：确保与 Commands/Hooks/Agents 页面的 UI 风格一致

---

## 📅 时间估计

| 阶段 | 预估工时 |
|------|----------|
| Phase 1: 后端数据模型 | 3-4h |
| Phase 2: 前端 Hooks | 2h |
| Phase 3: 核心组件 | 4-5h |
| Phase 4: 页面重构 | 3-4h |
| Phase 5: App.tsx 集成 | 1h |
| Phase 6: 清理与优化 | 1-2h |
| **总计** | **14-18h** |

---

*文档生成时间: 2026-01-06*
*基于访谈结果整理*
